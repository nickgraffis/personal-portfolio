const colors = require('tailwindcss/colors')

module.exports = {
  darkMode: 'class',
  theme: {
    colors,
    extend: {

    }
  },
  variants: {},
  plugins: [
    require('tailwind-nord'),
  ],
}
